#include <am.h>
#include <klib.h>

int identify_triggers(void);

int main() {
  if(identify_triggers()) return 1;
  return 0;
}

int identify_triggers(void){
  printf("Checking triggers.../n");
  //
}
